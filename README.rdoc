= Administrator

This is a plugin based on padrino framework.
Adminsitrator is an automatization to create an admin dashboard panel with authorization

== Usage

  # create your application
  $> rails new app ...
  
  # edit Gemfile
  gem 'administrator'

  # install the gem
  $> bundle install

  # run the generator
  $> rails g administrator
